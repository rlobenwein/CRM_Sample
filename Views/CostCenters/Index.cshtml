@model IEnumerable<RLBW_ERP.Models.FinanceModels.CostCenter>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.Raw("Centro de Custo")
            </th>
            <th>
                @Html.Raw("Cliente")
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opportunity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Notes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResultValue)
            </th>
@*              <th>
                @Html.DisplayNameFor(model => model.ResultPercentage)
            </th>
            *@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-action="Details" asp-controller="CostCenters" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.Id)</a>
                </td>
                <td>
                    <a asp-action="Details" asp-controller="Companies" asp-route-id="@item.Opportunity.CompanyId">@Html.DisplayFor(modelItem => item.Opportunity.Company.FriendlyName)</a>
                </td>
                <td>
                    <a asp-action="Details" asp-controller="Opportunities" asp-route-id="@item.OpportunityId">@Html.DisplayFor(modelItem => item.Opportunity.Id)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Notes)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                 <td>
                    @Html.DisplayFor(modelItem => item.ResultValue)
                </td>
@*                 <td>
                    @Html.DisplayFor(modelItem => item.ResultPercentage)
                </td>
            *@
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
