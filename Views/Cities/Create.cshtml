@model RLBW_ERP.Models.LocationModels.City

<div class="container">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-action="Create" class="myForm" method="post">
                <div class="modal-header">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="StateId" />
                    <h5>Inserir cidade no estado/província: @ViewData["SelectedState"] (@ViewData["SelectedCountry"])</h5>
                    <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
                    <hr />
                </div>
                <div class="modal-body">
                    <div class="input-group mb-3">
                        <span class="input-group-text"> @Html.DisplayNameFor(modelItem => Model.Name)</span>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <input id="saveButton" type="submit" value="Salvar" class="btn btn-sm btn-outline-secondary" />
                    <button type="button" class="btn btn-sm btn-outline-secondary" data-dismiss="modal">Fechar</button>
                </div>

            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
